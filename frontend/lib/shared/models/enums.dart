enum UserRole { nurse, doctor, admin }

enum TriageRoom { red, yellow, green, black }

enum TriageColor { red, yellow, green, black }

enum PatientStatus { waiting, inTreatment, discharged, deceased }

enum Sex { male, female }

enum AgeRange { infant, child, adult, elder }
